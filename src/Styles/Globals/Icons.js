import styled from "styled-components";

export const ContentIconsGlobal = styled.div`
  text-align: end;
  color: ${ props => props.theme.colors.light};
  font-size: 24px;
  position: relative;
  svg {
    vertical-align: middle;
  }
`